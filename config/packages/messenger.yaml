framework:
    messenger:
        transports:
            iFood:
                dsn: "doctrine://default"
                options:
                    table_name: messenger_messages
                    queue_name: iFood
        routing:
            'ControleOnline\Message\iFood\OrderMessage': iFood
