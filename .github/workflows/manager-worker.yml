name: Manager Worker

on:
  workflow_dispatch:

# PAUSA CTO ControleOnline/agents-mcp#201 (2026-08-29)
# Trigger original desativado: push em master, dev, staging.

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  manager:
    name: Manager — paused
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manager Worker paused by ControleOnline/agents-mcp#201"
